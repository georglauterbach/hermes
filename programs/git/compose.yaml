# docker compose up --build
services:
  git-builder:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: git-builder
    volumes:
      - ./out:/out

    #tty: true
    #stdin_open: true
